<%= render(SpacingWrapper.new) do %>
  <% inputs.each do |input| %>
    <% if input.renderable? %>
      <%= render(input) %>
    <% else %>
      <%= render(input.to_component) %>
    <% end %>
  <% end %>
<% end %>
<% if after_content? %>
  <%= render_after_content %>
<% end %>
